package poker;

/**
 * Created by jacobgoodwin on 4/26/17.
 */


public class Database {
    public static void main(String[] args) {
        /*try {
            //connection to database
            Connection myConn = DriverManager.getConnection("jdbc:mysql://localhost:8889/java", "root", "root"); //create statement Statement myStmt = myConn.createStatement(); //execute sql query
            Statement myStmt = myConn.createStatement();
            ResultSet myRs = myStmt.executeQuery("select * from users"); //results set
            while (myRs.next()) {
                System.out.println(myRs.getString("username") + " , " + myRs.getInt("gold") + " , " + myRs.getInt("handsplayed"));
            }
        } catch (Exception exc) {
            exc.printStackTrace();
        }*/

    }
}